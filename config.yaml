account:
  id: "082401002764"
  bank: "ICICI"
  branch: "Barasat"
  name: "ICICI New Savings Ltd."

matching:
  date_tolerance_days: 3
  carry_forward_date_tolerance_days: 5
  gib_date_tolerance_days: 1
  bil_date_tolerance_days: 1
  stale_carry_forward_days: 90

database:
  url: "postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres?sslmode=require"
  fallback_sqlite_path: "db/brs.db"

output:
  folder: "output"
